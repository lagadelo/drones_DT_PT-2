# Variant C: when locally balanced, drone relaxes speed progressively to nominal

n_initial=20
n_total=40
perimeter=100

d_star=5
V=1.0
Vmax=1.5

controller_mode=3
k_sym=0.10
# consider balanced when |gap_f-gap_b| <= 0.5m
balanced_gap_eps=0.5
# per-step relaxation toward target (0..1); smaller => slower decay
speed_relax_rate=0.05

steps=3000
dt=0.1
seed=1

resilience=1
num_losses=15
max_spares=15
loss_to_spare_delay_min_steps=100
loss_to_spare_delay_max_steps=300
spare_interval_min_steps=50
spare_interval_max_steps=150
incoming_hold_steps=50
incoming_v=1.0
